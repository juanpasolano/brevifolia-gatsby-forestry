---
label: Layout
hide_body: false
display_field: title
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: blocks
  type: blocks
  template_types:
  - site-info
  config:
    min: 
    max: 
  label: Blocks
